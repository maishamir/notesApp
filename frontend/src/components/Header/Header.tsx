import ThemeToggle from "../ThemeToggle/ThemeToggle";

type Props = {};

function Header({}: Props) {
  return (
    <div>
      Header
      <ThemeToggle />
    </div>
  );
}

export default Header;
